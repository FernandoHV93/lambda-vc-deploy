# Chatterbox (modelo único) — alineado con chatterbox-tts 0.1.6
torch==2.6.0
torchaudio==2.6.0
numpy>=1.24.0,<1.26
chatterbox-tts==0.1.6

# RunPod / S3 / backend
runpod>=0.10.0
boto3>=1.26.0
botocore>=1.29.0

# Audio y utilidades
soundfile>=0.12.1
pydub>=0.25.1
tqdm>=4.65.0
requests>=2.28.0
cached-path>=1.3.0

# Tests (opcional)
pytest>=7.0.0
scipy>=1.10.0
matplotlib>=3.7.0
